/**
 * 가계도 타이포그래피 상수.
 * 모든 폰트 관련 값은 이 파일을 단일 출처(SSOT)로 참조한다.
 */

// ── 폰트 크기 (px) ──

/** 노드 이름, 상세 텍스트, 나이 등 기본 라벨 */
export const FONT_SIZE_XS = 20;

/** 생몰연도 라벨 */
export const FONT_SIZE_XXS = 16;

/** 어노테이션 텍스트 */
export const FONT_SIZE_ANNOTATION = 22;

/** 미지정 성별 ? 표시 */
export const FONT_SIZE_SYMBOL = 18;

// ── 라인 높이 ──

/** 어노테이션 텍스트 라인 높이 */
export const LINE_HEIGHT_ANNOTATION = 1.4;

/** 일반 텍스트 라인 높이 */
export const LINE_HEIGHT_DEFAULT = 1.5;

// ── Tailwind 유틸리티 대체 참조 ──
// text-xs = 12px (FONT_SIZE_XS)
// text-[10px] = 10px (FONT_SIZE_XXS)
