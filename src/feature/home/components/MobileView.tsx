import React from 'react';

import { Button } from '@/components/ui';

const MobileView = () => {
  return (
    <div className="px-6 pt-[72px]">
      <Button>녹음 파일 업로드하기</Button>
    </div>
  );
};

export default MobileView;
